import * as theme from '../../theme';

export const style = {
  bg: theme.CARD_BG,
  border: {
    fg: theme.CARD_BORDER_FG
  }
};

export const element = {
  tags: true,
  border: {
    type: 'line'
  }
};
