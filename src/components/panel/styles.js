import * as theme from '../../theme';

export const style = {
  border: {
    fg: theme.PANEL_BORDER_FG
  }
};

export const element = {
  tags: true,
  border: {
    type: 'line'
  },
  padding: {
    top: 1,
    right: 2,
    bottom: 1,
    left: 2
  }
};
